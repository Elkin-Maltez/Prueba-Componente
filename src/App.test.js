//import { render, screen } from '@testing-library/react';

import React from 'react';
import { shallow, mount } from "enzyme";

// import { Contrato } from './Componentes/contrato';
// describe('Datos guardados',()=>{
//   it('1. Cargar datos en la pantalla Contrato', () => {
//     shallow(<Contrato/>);
//   });
// })

// import {Empleado} from './Componentes/empleado';
// describe('Datos guardados',()=>{
//   it('2. Cargar datos en la pantalla Empleado', () => {
//      shallow(<Empleado/>);
//   });
// })


// import {Pagos} from './Componentes/pago';
// describe('Datos guardados',()=>{
//   it('3. Cargar datos en la pantalla Pago', () => {
//     shallow(<Pagos/>);
//  });
// })




/* test('renders learn react link', () => {
  render(<App />);
  const linkElement = screen.getByText(/learn react/i);
  expect(linkElement).toBeInTheDocument();
});
 */

/* import Login from './Componentes/login';
describe('Login',()=>{
  it('1. Validar campo email ingresado de la manera correcta', () => {
    const wrapper= shallow(<Login/>);
    const pass = wrapper.find("email")
    expect(pass.type== "email")
  }) 
  it('2. Validar campo contraseña ingresada se convierta a asterisco', () => {
    const wrapper= shallow(<Login/>);
    const pass = wrapper.find("password")
    expect(pass.type== "password")
  })
  it('3. Levantar login sin errores', () => {
    shallow(<Login/>);
    
 });  
})
 */
// import {RepAverias} from './Componentes/averia'
// describe('Datos guardados',()=>{
//   it('1. Cargar datos en la pantalla Reporte Avería', () => {
//      shallow(<RepAverias/>);
//   });
// })
